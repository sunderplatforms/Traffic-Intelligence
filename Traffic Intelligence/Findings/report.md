feature importance rankings were sensitive to the Random Forest's max_features hyperparameter: max_features=None allowed a single dominant feature (count_point_id) to monopolize splits, while max_features='sqrt' distributed importance more evenly and reduced the model's reliance on location specifically. This indicates that feature importance conclusions in tree ensembles are not a fixed property of the data alone, but interact with hyperparameter choices — a caveat rarely discussed in applied feature-importance analyses.